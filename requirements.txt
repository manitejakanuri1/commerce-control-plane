fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
# razorpay 1.x imports pkg_resources, which setuptools 81+ removed and
# Python 3.13 does not provide. 2.x dropped that dependency.
razorpay==2.0.1
# create_index_for_model (integrated embeddings) needs pinecone 6+.
pinecone>=7.0.0
# DeepSeek speaks the OpenAI protocol, so the openai client reaches it with a
# different base URL.
openai==1.59.6
pytest==8.3.4
httpx==0.28.1
